import { getFrameImageBuffer } from "./generateFrameImageBuffer";
export async function getBase64FrameImage(element, satoriOptions, outputOptions) {
    const jpegBuffer = await getFrameImageBuffer(element, satoriOptions, outputOptions);
    const jpegBase64 = jpegBuffer.toString("base64");
    return `data:image/jpeg;base64,${jpegBase64}`;
}
//# sourceMappingURL=getBase64FrameImage.js.map