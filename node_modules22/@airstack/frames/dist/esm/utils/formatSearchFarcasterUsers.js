export function formatSearchFarcasterUsers(data) {
    var _a, _b, _c;
    return ((_c = (_b = (_a = data === null || data === void 0 ? void 0 : data.Socials) === null || _a === void 0 ? void 0 : _a.Social) === null || _b === void 0 ? void 0 : _b.map(({ profileName, fnames, custodyAddress, connectedAddresses, userAssociatedAddresses, followerCount, followingCount, fid, profileImageContentValue, }) => {
        const { image: profileImage } = profileImageContentValue !== null && profileImageContentValue !== void 0 ? profileImageContentValue : {};
        return {
            profileName,
            fnames,
            custodyAddress,
            connectedAddresses,
            userAssociatedAddresses,
            followerCount,
            followingCount,
            fid,
            profileImage,
        };
    })) !== null && _c !== void 0 ? _c : []);
}
//# sourceMappingURL=formatSearchFarcasterUsers.js.map