/// <reference types="node" />
import type { SatoriOptions } from "satori";
import { ReactElement } from "react";
export declare function getFrameImageBuffer(element: ReactElement, satoriOptions: SatoriOptions, outputOptions?: {
    width?: number;
    quality?: number;
}): Promise<Buffer>;
