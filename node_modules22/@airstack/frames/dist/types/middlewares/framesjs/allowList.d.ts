import type { FramesMiddleware } from "../../types";
/**
 *
 * @param input
 * @returns
 */
export declare const allowListFramesjsMiddleware: (input: any) => FramesMiddleware<any, {
    isAllowed: boolean;
}>;
